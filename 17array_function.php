<?php
// array methods
$a = ["this", "that", "what", "how", "that", "what"];
$users = ['paul', 'camel', 'henry', 'trippy', 'sam'];
// $users = ['name'=> 'sam', 'age'=>54, 'city'=>'delhi'];
// $users = 'hello';
// echo is_array($users); // for checking if it is an array or not

// if(is_array($users)){
//     echo "this is an array";
// }else{
//     echo "this is not an array";
// }

// echo count($users); // for counting elements of an array
// unset($users[3]); // for removing an element from the array
// array_push($users, 'tom'); // for adding an element at the end of an array
// array_pop($users); // for removing last element
// print_r(array_keys($users)); // for printing array keys
// print_r(array_merge($users, $a)); // for merging two arrays
// $dt = array_unique($a); // for deleting duplicate elements
// print_r($dt);
// print_r($users);

// echo implode($users); // for converting array into string
// $str = "hellow how are you mate";
// print_r(explode(' ', $str)); // for converting string into array
// print_r(explode('o', $str)); // whatever you write in the qoutes on that basis string will be converted into array as above string is converted on the basis of space in the string.
?>